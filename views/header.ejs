<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		
		<title>Library Management System</title>
		<link rel="stylesheet" href="/style.css" />
	</head>
	<body>
		
		<!-- <header>
			<div class="logo">
                <img width='50' height='50' src="/img/logo.png">
				<a href="/"><h1>Library</h1></a>
			</div>
			<nav>
				<ul>
				
					<% if(user) { %>
							
						<% if(user.role==='user'){ %>
							<li><a href="/cart">Cart(<%=user.cart.length %>)</a></li>
							<% } %>
					
					<li>
						<div class="dropdown">
							<button class="dropbtn">Hello <%= user.name%>
							<% if(user.messages.length!=0){ %>
                                <h1>H</h1>
								<% } %>
							
							</button>
							<div class="dropdown-content">
								<a href="/me">Account</a>
								
								<% if(user.role==='user'){ %>
									<a href="/messages">Messages

										<% if(user.messages.length!=0){ %>
											<h1>H</h1>
											<% } %>
									</a>
									<% } %>
								<a class="logout">Logout</a>
							</div>
						</div>
					</li>
					<% }else{ %>
					<li><a href="/login">Login</a></li>
					<li><a href="/signup">SignUp</a></li>
					<% } %>
				</ul>
			</nav>
		</header>  
		 -->

		 <header>
			<div class="logo">
                <img width='55' height='55' src="https://img.icons8.com/external-flaticons-flat-flat-icons/344/external-library-university-flaticons-flat-flat-icons-3.png">
				<a href="/"><h1 style="padding-top: 10px; margin-left: 10px;">Library</h1></a>
			</div>
			<nav>
				<ul>
				
					<% if(user) { %>
							
						<% if(user.role==='user'){ %>
							<li><button class="navcartbtn"><a href="/cart">Cart(<%=user.cart.length %>)</a></button></li>
							<% } %>
					
					<li>
						<div class="dropdown">
							<button class="dropbtn">	<% if(user.messages.length!=0){ %>
								<div class="circle"></div>
								<% } %>Hello <%= user.name%></button>
							<div class="dropdown-content">
								<a href="me">Account <img style="margin-left: 10px; height: 15px;width : 15px" src="https://img.icons8.com/external-flaticons-flat-flat-icons/344/external-library-university-flaticons-flat-flat-icons-3.png" /></a><br><br>
								<% if(user.role==='user'){ %>
									<% if(user.messages.length!=0){ %>
									<div class="circle"></div>
									<% } %>
									<a href="/messages">Messages <img style="margin-left: 3px; height: 15px;width : 15px" src="https://cdn-icons-png.flaticon.com/512/561/561127.png"> </a> <br><br>
									<% } %>
								<a class="logout">Logout <img style="margin-left: 10px; height: 15px;width : 15px" src="https://cdn-icons-png.flaticon.com/512/992/992680.png"> </a>
							</div>
						</div>
					</li>
					<% }else{ %>
					<li><a href="login">Login</a></li>
					<li><a href="signup">SignUp</a></li>
					<% } %>
				</ul>
			</nav>
		</header>